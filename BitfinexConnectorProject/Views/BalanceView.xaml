<Page x:Class="BitfinexConnectorProject.Views.BalanceView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:BitfinexConnectorProject.Views"
      xmlns:vm="clr-namespace:BitfinexConnectorProject.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="BalanceView"
      FontSize="24"
      FontFamily="Calibri">

    <Page.DataContext>
        <vm:BalanceViewModel/>
    </Page.DataContext>
    
    <Grid
        Background="AliceBlue">

        <Grid.RowDefinitions>
            <RowDefinition Height=".2*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Label
            Content="Total coins: 1 BTC, 15000 XRP, 50 XMR, 30 DASH"
            Margin="10 10 0 0"/>

        <Grid Grid.Row="1"
              Margin="10 0 0 0">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width=".3*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Label
            Grid.Row="0"
            Content="Balance in USDT:"
            VerticalAlignment="Center"/>
        <Label
            Grid.Row="1"
            Content="Balance in BTC:"
            VerticalAlignment="Center"/>
        <Label
            Grid.Row="2"
            Content="Balance in XRP:"
            VerticalAlignment="Center"/>
        <Label
            Grid.Row="3"
            Content="Balance in XMR:"
            VerticalAlignment="Center"/>
        <Label
            Grid.Row="4"
            Content="Balance in DASH:"
            VerticalAlignment="Center"/>

            <Label
                Grid.Row="0"
                Grid.Column="1"
                Content="{Binding BalanceUsdt}"
                VerticalAlignment="Center"/>


        </Grid>
    </Grid>
</Page>
